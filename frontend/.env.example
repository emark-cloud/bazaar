# Bazaar frontend env. Copy to .env.local (gitignored) and fill in.
#
# Deployed Bazaar subgraph GraphQL endpoint. When unset, the UI reads everything
# from the chain directly (match history / P&L are hidden).
#
# Live self-hosted subgraph (graph-node on the helios VPS, fronted by nginx+ZeroSSL TLS):
#   https://bazaar.38.49.214.20.sslip.io/subgraphs/name/bazaar
# (Both hosted services — Ormi, Protofire — were down; see indexer/README.md "Self-host on a VPS".)
VITE_SUBGRAPH_URL=https://bazaar.38.49.214.20.sslip.io/subgraphs/name/bazaar
